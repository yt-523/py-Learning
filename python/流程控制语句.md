## 条件判断
if格式：
if 要判断的条件：                  //判断语句的结果必须为布尔值
	操作                              //操作要有缩进，条件为True

如果条件为False
else:
	操作

也可以使用elif进行多区间条件判断

例子：
if 0<a&&a<60:
	print("不合格")
elif a<80:
	print("良好")
else:
	print("优秀")
![[Pasted image 20260909213534.png|281]]